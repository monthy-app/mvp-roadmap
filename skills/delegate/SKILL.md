---
name: delegate
description: Delegate implementation work to subagents on the cheapest model tier that can do each job well, keeping design, review, and synthesis in the main loop. Not yet implemented; this is a specified placeholder that lands with its roadmap epic.
---

# delegate — placeholder

Specified but not yet implemented; the spec is the "delegate" section of this repo's PRD, and the roadmap tracks when it lands.

Until then, do the equivalent inline: specify the change precisely first, hand well-scoped implementation to a lower-tier subagent (a capable mid-tier model for substantive coding, the smallest tier for mechanical edits), and verify the result yourself — typecheck, lint, test, read the diff. Judgment stays in the main loop; the subagent only types.
