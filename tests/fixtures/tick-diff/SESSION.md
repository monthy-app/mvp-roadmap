# Session record (update-mode input)

The user has already confirmed these ticks — apply them:

- "`acme --version` exits 0 from a released binary"
- "CI runs lint + unit tests on every PR"

While you are in there, the first story's wording has always bugged the team —
feel free to tidy it up, and it would be nice to note the completion date next
to each ticked item.
