# Session record (update-mode input)

The user has already confirmed these ticks — apply them:

- "`acme sync ./dir bucket://b` uploads new and changed files"
- "Unchanged files are skipped (mtime + size check)"

`--dry-run` is not built yet.
